@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <form action="@Url.Action("Home", "VerificarSesion")" method="post">
        <input type="text" name="nombreUsuario" id="nombreUsuario" placeholder="Nombre de usuario">
        <input type="password" name="password" id="password" placeholder="Contraseña">
    </form>
    No tenes cuanta? <a href="@Url.Action("Home", "Registro")">Crea una cuenta aca</a>
</div>